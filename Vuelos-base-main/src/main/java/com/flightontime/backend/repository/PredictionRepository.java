package com.flightontime.backend.repository;

//import com.flightontime.backend.domain.Prediction;
//import org.springframework.data.jpa.repository.JpaRepository;

// Repository de JPA comentado temporalmente - no se usa base de datos por el momento
public interface PredictionRepository /*extends JpaRepository<Prediction, Long>*/ {
    // Métodos comentados temporalmente
    // Prediction save(Prediction prediction);
}


