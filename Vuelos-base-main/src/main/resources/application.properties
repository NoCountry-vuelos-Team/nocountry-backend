# Configuración común para todos los ambientes
spring.application.name=Prototipo

# Configuración de Swagger (común, puede ser sobrescrita por perfiles específicos)
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.tryItOutEnabled=true
springdoc.swagger-ui.displayRequestDuration=true

# Perfil activo por defecto (puede ser sobrescrito por variable de entorno)
# spring.profiles.active=local
